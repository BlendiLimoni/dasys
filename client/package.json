{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html --host 0.0.0.0",
    "build": "rm -rf build && parcel build src/index.html --dist-dir build --no-scope-hoist --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "socket.io-client": "^4.7.4"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "postcss": "^8.4.38",
    "@parcel/transformer-postcss": "^2.12.0"
  }
}
